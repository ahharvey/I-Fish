<%= simple_form_for(@graticule, html: { class: 'form-horizontal'})  do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :code %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
