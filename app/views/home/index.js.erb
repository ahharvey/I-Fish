

$('.timeline').append('<%= j render partial: "activities/activity", collection: @activities, locals: { view: self }  %>');
<% if @activities.last_page? %>
  $('.pagination').remove();
<% else %>
  $('.pagination').replaceWith('<%= j render "pagination" %>');
<% end %>