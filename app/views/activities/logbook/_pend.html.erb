<% if logbook %>
  marked a logbook as pending:
  <br />
  <%= link_to "#{logbook.user.desa.name} - #{logbook.logbook_date}", logbook %>
<% else %>
  marked as pending a logbook that has since been deleted
<% end %>