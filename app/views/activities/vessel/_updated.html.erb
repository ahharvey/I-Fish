<% if vessel %>
  updated a registered vessel : <%= link_to "#{vessel.name} (#{vessel.ap2hi_ref})", vessel %>
  <br />
  <span class="fa-stack fa-lg">
    <i class="fa fa-square fa-stack-2x text-info"></i>
    <i class="fa fa-edit fa-stack-1x fa-inverse"></i>
  </span>
<% else %>
  updated a registered vessel that has since been deleted
<% end %>
