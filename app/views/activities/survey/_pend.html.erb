<% if survey %>
  marked a survey as pending:
  <br />
  <%= link_to "#{survey.desa.name} - #{survey.formatted_date}", survey %>
<% else %>
  marked as pending a survey that has since been deleted
<% end %>