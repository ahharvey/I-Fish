<% if survey %>
  marked a survey as pending : <%= link_to "#{survey.desa.name} - #{survey.formatted_date}", survey %>
  <p><span class="label label-warning"><i class='icon-warning-sign icon-2x'></i></span></p>
<% else %>
  marked as pending a survey that has since been deleted
<% end %>