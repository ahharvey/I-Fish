<% title "Roles" %>

<%= render partial: 'role_list' %>

<%= form_for [:panel, @new_role] do |f| %>
  <%= f.label :name, 'Name of new role:' %>
  <%= f.text_field :name %>
  <%= f.submit %>
 <% end %>