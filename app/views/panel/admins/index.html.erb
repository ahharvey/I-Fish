<% title "Admins" %>

<table class="table table-striped table-condensed">
  <tr>
  	<th>Name</th>
  	<th></th>
  </tr>
  <% for admin in @admins %>
  <tr>
  	<td><%= link_to admin.name, [:panel, admin] %></td>
  	<td></td>
  </tr>
  <% end %>
 </table>